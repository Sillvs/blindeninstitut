# OpenAI API key — required for /api/extract, /api/generate, /api/chat
# Store in your hosting provider's secret manager, never commit.
OPENAI_API_KEY=

# Bearer-token gate for /api/* routes. REQUIRED in production.
# Generate with: openssl rand -hex 32
# When unset, /api/* is OPEN (dev convenience only).
API_TOKEN=
